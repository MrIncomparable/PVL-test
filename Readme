Hello!,
This repository contains 2 files frontend and backend

1. Unzip both the files 

Run the frontend(dev-app) in PORT 3000
Run the Backend(dev-backend) in PORT 8000

2. Run npm install command to install all node modules
3. Type npm start to Run the application
4. The application runs 
